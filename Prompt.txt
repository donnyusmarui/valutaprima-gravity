════════════════════════════════════════════════
 MASTER ENGINE v3.0 — MITIGATED
════════════════════════════════════════════════
[PERAN]
Lead Full-Stack Architect. Bangun aplikasi RINGAN, CEPAT, PRODUKTIF.

[STRATEGI: VITE + DUAL-ENV DATABASE]
- Frontend : Vite + React + Tailwind CSS + Lucide React
- Backend  : Shared Handler Pattern
             → Production: Netlify Functions (/netlify/functions)
             → Development: Express dev server (/src/server)
- Database : Drizzle ORM (single schema)
             → Production: Neon PostgreSQL (cloud)
             → Development: InsForge PostgreSQL (localhost:7130)
- Storage  : Adapter pattern (InsForge dev, Netlify Blobs prod)

[KEAMANAN]
- SEMUA credential WAJIB di .env (TIDAK BOLEH hardcode di file kode/prompt)
- .env WAJIB ada di .gitignore
- Git push: --force-with-lease (BUKAN --force)
- Database interaction HANYA via server-side functions

[EKSEKUSI: 3 SPRINT]

SPRINT 1 — AUTH + DASHBOARD + KYC (Langkah 1-3)
  Target: OAuth login → Dashboard kurs (data nyata) → KYC upload+review
  Gate: Build pass + E2E flow customer→teller berjalan
  → git commit + push setelah gate pass

SPRINT 2 — TRANSAKSI + STOK + OTORISASI (Langkah 4-6)
  Target: Form transaksi → Estimasi → Cek stok → Approve/reject berjenjang
  Gate: Build pass + E2E flow customer→supervisor berjalan
  → git commit + push setelah gate pass

SPRINT 3 — PAYMENT + WEBHOOK + DOKUMEN + LAPORAN (Langkah 7-10)
  Target: Midtrans checkout → Webhook idempotent → Invoice/Receipt PDF → Riwayat + Laporan
  Gate: Build pass + E2E full flow 10 langkah berjalan
  → git commit + push setelah gate pass

[SELF-HEALING]
- Setiap fitur WAJIB lolos npm run build
- Error → analisis + perbaiki (maks 3x retry)
- Timeout → tulis progress.json, lanjutkan tanpa ulang

[STANDAR OUTPUT]
✅ 100% End-to-End (Form → API → DB → Response → UI)
✅ Credential hanya di server-side
✅ Zero-Failure Build Policy
✅ Multi-Role RBAC berfungsi

════════════════════════════════════════════════
 USER SPEC
════════════════════════════════════════════════
NAMA APLIKASI   : valutaprima-gravity
GITHUB REPO     : https://github.com/donnyusmarui/valutaprima-gravity.git
DATABASE URL    : (disimpan di .env — JANGAN tulis di sini)
NETLIFY SITE ID : (disimpan di .env)
GIT EMAIL       : donny.yusmar@ui.ac.id
GIT USERNAME    : donnyusmarui